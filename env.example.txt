# Supabase Local Development
# Obtén estos valores ejecutando: pnpm supabase:start

# URL de la API local de Supabase
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54361

# Clave pública (anon key) - obtenida del comando supabase:start
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=tu_anon_key_aqui

# App Configuration
NEXT_PUBLIC_APP_NAME=Lettering Shop

# INSTRUCCIONES:
# 1. Copia este archivo como .env.local
# 2. Ejecuta: pnpm supabase:start
# 3. Copia el "anon key" que se muestra en la terminal
# 4. Reemplaza "tu_anon_key_aqui" con el valor real
